# Changelog

## 2026-05-11 · v2 原型更新

### 推送内容

- 新增当前主演示文件 `worldcup-prototype-v2.html`。
- 更新 `README.md` 当前版本入口、预览地址和 SHA1。
- 当前 HTML SHA1：`db789ce0343fb51a984608eafd2f680a19f46a71`。

### 本轮重点

- 顶部任务按钮打开全量应援任务弹窗，底部 Tab 回到 `看台 / 球场 / 媒体` 三入口。
- 1F 球迷证三按钮对齐 PRD：环球世界、射门助力、焦点竞猜均进入用户个人相关弹窗。
- 清理可见 `TBD / placeholder / 跳转占位`，保留代码内 PRD 元数据。
- 看台人浪使用内联黑白球场看台底图，电子屏承载 TOP 国家滚动展示。
- 我是教练补齐环球世界收集弹窗、国家文化卡片和 demo 链接入口。

## 2026-05-10 · 23:40 归档更新

### 归档

- 同步当前最新 `worldcup-prototype.html` 到 `原型设计/`。
- 更新 `README.md` 和 `CHANGELOG.md`，补齐最新页面层级、跳转关系和 PRD 映射规则。
- 当前 HTML SHA1：`d0cac5678280e90919fff15eb328055d03c78240`。

### 全局展示

- 隐藏页面外部 Stage / JS API 说明区，避免演示时出现开发说明。
- 保留代码内 PRD 锚点和逻辑注释，方便后续从 demo 反推 PRD。
- UI 中不再显示无效红色 TBD 占位，相关信息尽量转入代码注释或 `data-prd-label`。

### FAN ID 与任务

- 删除 FAN ID 页顶部无效 Onboarding 卡片。
- FAN ID 卡片填入演示内容，避免主队、球星、贡献值为空。
- 应援任务替换为完整世界杯活动任务清单。
- 隐藏所有任务中的 HT 币 / RMB 红色占位和无效 `{number}`。
- 底部任务入口标题替换为 `应援任务 MATCH TASK`，并融入底部 Tab 栏。

### 媒体页

- `ON AIR` 正在直播模块改为视频卡结构，并接入 `Tab 4.1 · 直播语聊聚合页`。
- `CIRCLE` 世界杯圈子入口接入 `Tab 4.2 · 动态世界杯话题页`。
- `MEETUP` 线下观赛入口接入 `Tab 4.3 · 线下观赛页`，并补充详情半屏和报名表单。
- `HIGHLIGHTS` 精彩集锦修正为：入口卡 → 短视频集合页 → 观看视频页。
- `GROUP` 球迷群入口改为国家群半屏列表，并接入 `Tab 4.5 · 阿根廷球迷会` App 群聊页。

### 商店页

- VIP 年卡 / 终身卡点击后打开 SKU 半屏购买。
- 年卡赠送礼包和终身卡赠送礼包移入各自 SKU 半屏。
- 装扮商城、球队套系、限定礼物模块隐藏无效红色 TBD。
- 球队礼物套系显示为正式入口文案，不显示调试占位信息。

## 2026-05-10 · 收口版

### 全局结构

- 默认刷新进入 `Tab 3 · 球场`。
- 顶部 Home 图标作为 FAN ID 配置入口。
- 顶部 `FANID` 按钮进入 `Tab 1 · 入场`。
- 商店从底部 Tab 移出，改为右下角全局浮动入口。

### 底部 Tab 栏

- 底部 Tab 只显示黑白图标和名称，不显示数字。
- 隐藏保留 `2 / 3 / 4` 编号，便于 PRD 反推。
- 三个 Tab 按钮改为方形入口：`看台 / 球场 / 媒体`。
- Tab 栏集成全局「应援任务 ↑」入口。
- 应援任务入口去掉独立边框，视觉上融入底栏。
- 打开任务抽屉时，Tab 栏整体上移。

### 应援任务

- `m1-task` 在 FAN ID 页面原位置保持不动。
- 底部常驻入口复用同一份 `TASK_DATA`。
- 点击底栏任务区或上滑底栏，打开半屏任务抽屉。
- 任务抽屉一次性展示每日任务、一次性任务、不限次任务。

### 球场与射门

- `Tab 3 · 球场` 只保留进球助力入口。
- `Tab 3.1 · 射门助力玩法` 独立为同级页面切换。
- 射门助力页面包含活动礼物、球门玩法、奖励兑换和概率公式。
- 每次进入射门助力页面都会弹出「送你一个特别惊喜！射门*1」。

### 看台与教练

- 「我是教练」从九宫格占位升级为战术球场展示。
- 新增比分条、比赛时间、球场线框、球员点位和战术区点击反馈。
- 保留环球世界入口和 GOAL 消耗逻辑。

### 商店

- VIP 促销改为黑白风格双图轮播。
- 年卡展示限时 7 折、世界杯限量礼包、原价与现价。
- 终身 VIP 展示限时 7 折与折后价。
- 删除原 `VIP · LIFETIME` 独立模块卡。

### 弹窗与半屏交互

- `更多赛事` 半屏弹窗支持点击屏幕上半部分关闭。
- 半屏弹窗统一为遮罩层 + 底部面板结构。
- 任务抽屉使用半屏结构，但保留和底部 Tab 栏联动上移。

### PRD 反推锚点

- 底部 Tab 保留 `data-tab-code` 和 `data-prd-label`。
- 全局任务入口保留 `Global · 应援任务常驻入口 · 嵌入底部 Tab 栏`。
- 关键模块继续保留 `#m1-task`、`#m2-coach`、`#m3-goal`、`#m3-bet` 等模块 ID。
